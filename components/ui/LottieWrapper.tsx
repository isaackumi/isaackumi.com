'use client';

import Lottie from "lottie-react";
import type { LottieComponentProps } from "lottie-react";

export function LottieWrapper(props: LottieComponentProps) {
  return <Lottie {...props} />;
}